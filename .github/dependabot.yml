# The actions are pinned to commit hashes, so an update is a pull
# request against the branch the work happens on.
version: 2
updates:
- package-ecosystem: github-actions
  directory: /
  target-branch: dev
  schedule:
    interval: weekly
  commit-message:
    prefix: ci
